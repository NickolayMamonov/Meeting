<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Базовая XML-тема для системных окон (Activity до setContent, splash, диалоги).
        Все Material-цвета задаются на уровне Compose через UIKitTheme — здесь
        оставлен только прозрачный status bar, чтобы Compose-контент мог рисовать
        под системные панели.
    -->
    <style name="Theme.Meet.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Meet</item>
    </style>

    <style name="Theme.Meet" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>